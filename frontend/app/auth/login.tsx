/**
 * Login Route
 *
 * Route wrapper for LoginScreen component.
 * Accessible via /auth/login navigation.
 */

import { LoginScreen } from "../../components/Auth/LoginScreen";

export default function LoginRoute() {
  return <LoginScreen />;
}
