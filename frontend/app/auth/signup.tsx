/**
 * Signup Route
 *
 * Route wrapper for SignupScreen component.
 * Accessible via /auth/signup navigation.
 */

import { SignupScreen } from "../../components/Auth/SignupScreen";

export default function SignupRoute() {
  return <SignupScreen />;
}
